def vowel(str)
    count=0
    list = [a,e,i,o,u,A,E,I,O,U]
    for alpha in list:

        if alpha in list 
        count = count+1
        print("Number of vowel",count)

        argument str = ('There are 40 students in section A ')

        vowel(str)